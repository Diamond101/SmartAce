@model SmartAceModel.Books

@using (Ajax.BeginForm("_Delete", "Books", new AjaxOptions()
{
    Confirm = "Are you sure you want to delete Books Record?",
    LoadingElementId = "divLoading",
    OnBegin = "DeleteBegin",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "list",
    OnSuccess = "DeleteSuccessful",
    OnFailure = "Failed"

}))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.ID, new { @Id = "txtDeleteID" })

    <input type="submit" id="btnDelete" value="" style="display: none" />
}

